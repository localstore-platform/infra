# LocalStore API Server Configuration - CloudFlare Full (Strict) Mode
# 
# Uses CloudFlare Origin Certificate for end-to-end encryption.
# CloudFlare connects to origin via HTTPS.
#
# Setup:
#   1. Generate Origin Certificate in CloudFlare Dashboard:
#      SSL/TLS → Origin Server → Create Certificate
#   2. Save certificate as /opt/localstore/ssl/origin.pem
#   3. Save private key as /opt/localstore/ssl/origin-key.pem
#   4. Set CloudFlare SSL mode to "Full (Strict)"
#
# Domains per environment:
#   dev:     api-dev.localstore-platform.com
#   staging: api-staging.localstore-platform.com
#   prod:    api.localstore-platform.com

upstream api_backend {
    server api:8080;
    keepalive 32;
}

# TODO: Uncomment when ai-service is ready
# upstream ai_backend {
#     server ai:8000;
#     keepalive 16;
# }

# Redirect HTTP to HTTPS
server {
    listen 80;
    server_name ~^api.*localstore-platform\.com$ api.localstore-platform.com;
    return 301 https://$host$request_uri;
}

# HTTPS Server (CloudFlare Origin Certificate)
server {
    listen 443 ssl;
    http2 on;
    server_name ~^api.*localstore-platform\.com$ api.localstore-platform.com;

    # CloudFlare Origin Certificate
    ssl_certificate /etc/nginx/ssl/origin.pem;
    ssl_certificate_key /etc/nginx/ssl/origin-key.pem;

    # SSL settings optimized for CloudFlare
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
    ssl_prefer_server_ciphers off;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 1d;

    # Verify requests come from CloudFlare (optional but recommended)
    # CloudFlare IP ranges: https://www.cloudflare.com/ips/
    # Uncomment to restrict to CloudFlare IPs only:
    # allow 173.245.48.0/20;
    # allow 103.21.244.0/22;
    # allow 103.22.200.0/22;
    # allow 103.31.4.0/22;
    # allow 141.101.64.0/18;
    # allow 108.162.192.0/18;
    # allow 190.93.240.0/20;
    # allow 188.114.96.0/20;
    # allow 197.234.240.0/22;
    # allow 198.41.128.0/17;
    # allow 162.158.0.0/15;
    # allow 104.16.0.0/13;
    # allow 104.24.0.0/14;
    # allow 172.64.0.0/13;
    # allow 131.0.72.0/22;
    # deny all;

    # Real IP from CloudFlare
    set_real_ip_from 173.245.48.0/20;
    set_real_ip_from 103.21.244.0/22;
    set_real_ip_from 103.22.200.0/22;
    set_real_ip_from 103.31.4.0/22;
    set_real_ip_from 141.101.64.0/18;
    set_real_ip_from 108.162.192.0/18;
    set_real_ip_from 190.93.240.0/20;
    set_real_ip_from 188.114.96.0/20;
    set_real_ip_from 197.234.240.0/22;
    set_real_ip_from 198.41.128.0/17;
    set_real_ip_from 162.158.0.0/15;
    set_real_ip_from 104.16.0.0/13;
    set_real_ip_from 104.24.0.0/14;
    set_real_ip_from 172.64.0.0/13;
    set_real_ip_from 131.0.72.0/22;
    real_ip_header CF-Connecting-IP;

    # API routes
    location / {
        limit_req zone=api burst=20 nodelay;
        limit_conn conn 10;

        proxy_pass http://api_backend;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $http_x_forwarded_proto;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 300;
    }

    # TODO: Uncomment when ai-service is ready
    # location /ai/ {
    #     limit_req zone=api burst=10 nodelay;
    #     proxy_pass http://ai_backend/;
    #     proxy_http_version 1.1;
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    #     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #     proxy_set_header X-Forwarded-Proto $http_x_forwarded_proto;
    #     proxy_read_timeout 60;
    # }

    # Health check endpoint
    location /health {
        access_log off;
        proxy_pass http://api_backend/health;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
    }

    # Deny access to hidden files
    location ~ /\. {
        deny all;
    }
}
